# Ignore local development files or IDE-specific files
**/*.env
**/*.local
**/.vscode

# Ignore Fly.io or Heroku-related files with sensitive data
fly.toml
*.log

# Ignore Dockerfile if it’s only for local use (but NOT the production one)
# Remove this line if you need the Dockerfile in production:
# Dockerfile

# Node modules (if any)
node_modules

# Vendor directory (we're installing dependencies inside the container)
vendor/

# OS-specific or editor-specific junk files
**/.DS_Store

# Build artifacts
**/*.phar
**/*.zip

# PHPUnit and config cache files
**/.phpunit.result.cache
**/phpunit.xml
**/.php_cs
**/.php_cs.cache

# Remove duplicated or overly specific entries below if not applicable
